# from operators import addOperator as add

# from operators import divideOperator as divi

# import operators

from operators import addOperator
from operators import divideOperator

print(addOperator(4, 2))
print(divideOperator(4, 2))
